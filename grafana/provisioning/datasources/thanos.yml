apiVersion: 1

datasources:
  # Thanos Query speaks the Prometheus API, so Grafana treats it as a
  # Prometheus datasource. Nothing in Grafana knows Thanos exists.
  - name: Thanos
    type: prometheus
    uid: thanos
    access: proxy
    url: http://thanos-query:10902
    isDefault: true
